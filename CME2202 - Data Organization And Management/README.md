# CME2202 - Data Organization And Management

Data organization/file management course assignments (C).

| Folder | Description |
|---|---|
| `HW1 - Log Converter` | Converts/validates log records (`smartlogs.csv`) against an XML schema (`HW1.xsd`), configured via `setupParams.json`. |
| `HW2 - B+ Tree Indexing` | Builds a B+ tree index over sorted records (`final_sorted_records.csv`) for efficient lookup, with intermediate run files from an external sort. |
